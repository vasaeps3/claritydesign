export const ERRORS = {
  validations: {
    required: 'The field is required.',
    email: 'The field should be email (example@mail.com).',
    minlength: 'The field should have at least <%= requiredLength %> symbols.',
    maxlength: 'The field should be less or equal to <%= requiredLength %> symbols.',
    min: 'The field value should be more or equal to <%= min %>.',
    max: 'The field value should be less or equal to <%= max %>.',
    pattern: 'The username must contain only letters and numbers.'
  }
};
